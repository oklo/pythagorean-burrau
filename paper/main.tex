\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm,booktabs,hyperref}

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{conjecture}[theorem]{Conjecture}
\newcommand{\R}{\mathbb R}
\newcommand{\Bcal}{\mathcal B}

\title{Exact Reductions for the Pythagorean--Burrau\\
Nonperiodicity Conjecture}
\author{Working research draft}
\date{August 22, 2026}

\begin{document}
\maketitle

\begin{abstract}
We study free-fall solutions of the planar Newtonian three-body problem in
which the masses and their opposite side lengths are the same Pythagorean
triple.  The desired nonperiodicity theorem is not proved in this draft.  We
give exact reductions to primitive triples and a rational algebraic parameter
curve, prove equivalence with absence of a second brake, construct a
syzygy-valid three-component reduced brake residual, derive the universal
zero-angular-momentum reduced system, and verify exact initial identities.  We
also prove that the real isosceles-right endpoint has no second brake and ends
in finite collision. At the opposite singular endpoint, a Levi--Civita
argument proves that every sufficiently skinny member avoids the limiting
heavy-pair collision on its first encounter, with explicit miss-distance and
angular-momentum asymptotics. We derive the later rectilinear restricted
scattering limit and prove that it has a parabolic separatrix, ruling out a
phase-uniform strict-escape shortcut. We derive the exact collapsed-pair phase
clock and the finite-parameter field through second order, exposing a
parabolic time-translation gauge in the candidate splitting integral. A
rigorous interval-Taylor computation proves the remaining finite Jacobi sign
uniformly on an explicit launch-speed interval. An analytic compactification
then verifies McGehee's degenerate stable-manifold hypotheses and proves that
the limiting incoming and outgoing parabolic curves cross transversely.
Radius-dependent tail estimates and exact Levi--Civita normal invariants then
match the true finite-parameter state to the restricted incoming curve.
Consequently infinitely many open skinny parameter intervals, and hence
infinitely many primitive Pythagorean triples, are nonperiodic. An exact
equidistribution argument strengthens this to a positive-lower-density subset
of the explicit family $(4n^2-1,4n,4n^2+1)$. On the complementary restricted
side we prove infinitely many first-turn brake-state resonances, showing that
finite-parameter resonance splitting, rather than a uniform limiting
no-brake lemma, is required. We then validate the transverse scattering sign
and prove that normalized outer angular momentum stays nonzero at the first
outer turn uniformly throughout this accumulating resonance layer. For the
next compact encounter we prove a positive centered-parabolic endpoint, and
at its opposite triple-collision endpoint we prove uniqueness and collision
safety of the universal peel-off base and validate its positive slow-mode
first-turn transfer. In the remaining planar joint-limit family, an analytic
stable-tail enclosure and correlated interval-Newton computations prove two
distinct transverse light--heavy collision parameters, one for each primary,
in explicit local boxes.
An exact collision-adapted finite-mass reduction and a differentiated
two-mode inclination theorem continue this root to a normalized collision
graph through the logarithmic saddle dwell; the exact tied curve is reduced
locally to one scalar signed gap from that graph.  Combining its continuity
with the exact rapidly winding skinny clock proves that infinitely many
distinct real tied parameters have a finite-time classical collision, though
none of these isolated parameters is proved rational.  The certified limiting
collision--ejection continuation also crosses the following heavy-binary
collision into a strict escape cone.  A hierarchical Jacobi-energy bootstrap
transfers this to a finite-mass local collision-or-escape theorem.  Hence
every sufficiently late phase bracket either contains an earlier collision or
contains an open tied collision-or-escape interval.
The full conjecture remains open because the compact
phase interior, the one-parameter planar scattering family exposed by the
finite-mass triple-endpoint blow-up, and later turns are not yet controlled. A
bounded catalog audit finds
a close numerical periodic brake orbit on the
$3{:}4{:}5$ mass slice, but its initial triangle is not the exact tied right
triangle.
\end{abstract}

\section{Exact conjecture}

Let $a^2+b^2=c^2$ with positive integers, set
$(m_1,m_2,m_3)=(a,b,c)$, and put
\[
q_1=(-c/2,0),\quad q_2=(c/2,0),\quad
q_3=((b^2-a^2)/(2c),ab/c),\qquad \dot q_i=0.
\]
The side opposite each mass has the same length as that mass.  The conjecture
asserts that no classical solution has a collision-free labelled period.
Returns modulo rotation or label exchange and regularized continuation after
collision are excluded.

\section{Scaling and Euclid parameter}

\begin{lemma}[Simultaneous mass--length scaling]
If $q_i(t)$ solves the equations with masses $m_i$, then for $k>0$
\[
\widetilde m_i=km_i,\qquad \widetilde q_i(t)=kq_i(t/k)
\]
solves the rescaled problem.  A labelled period $T$ corresponds exactly to a
period $kT$.
\end{lemma}
\begin{proof}
Both sides of Newton's acceleration equation acquire the factor $k^{-1}$.
Velocity is unchanged after the corresponding time substitution, and mutual
distances acquire the positive factor $k$.
\end{proof}

It follows that only primitive triples matter.  For $u\in(0,1)$ define
\[
A(u)=\frac{1-u^2}{1+u^2},\qquad
B(u)=\frac{2u}{1+u^2}.
\]
If $u=p/q$ is reduced, Euclid's integer triple is
$(q^2-p^2,2pq,q^2+p^2)$ and its common divisor is two exactly when $p,q$ are
both odd.  Conversely Euclid's theorem recovers every ordered primitive
triple.  The inverse is $u=B/(1+A)$, and leg exchange is the involution
$u\mapsto(1-u)/(1+u)$.  Its fixed point is $\sqrt2-1$, giving the fundamental
interval $0<u\leq\sqrt2-1$ for the real family.

After normalizing $c=1$, masses and positions are
\[
(A,B,1),\qquad q_1=(-1/2,0),\quad q_2=(1/2,0),\quad
q_3=((B^2-A^2)/2,AB).
\]

\section{Second brakes and a quotient residual}

\begin{lemma}[Second-brake lemma]
A classical free-fall segment generates a collision-free labelled periodic
solution if and only if all inertial velocities vanish at some positive time
before collision.
\end{lemma}
\begin{proof}
Uniqueness and time reversal give $q(-t)=q(t)$.  A brake at $\tau$ gives
$q(\tau+s)=q(\tau-s)$ and hence a labelled period $2\tau$.  Conversely, a
labelled period $T$ combined with the symmetry at zero gives reflection
symmetry about $T/2$, and therefore zero velocity there.  The period $2\tau$
need not be minimal unless $\tau$ is the first positive brake.
\end{proof}

In the center-of-mass frame define Jacobi vectors
\[
X=q_2-q_1,\qquad
Y=q_3-\frac{m_1q_1+m_2q_2}{m_1+m_2}
\]
and Hopf invariants
\[
h(X,Y)=\left(\frac{|X|^2-|Y|^2}{2},X\cdot Y,X\times Y\right).
\]
We use the quotient residual
\[
\Bcal(u,t)=\frac{d}{dt}h(X(u,t),Y(u,t)).
\]
Away from triple collision, $Dh$ has rank three and kernel equal to the common
rotation direction.  Since total linear and angular momentum vanish,
$\Bcal=0$ is equivalent to vanishing of every labelled inertial velocity.
Unlike three mutual-distance derivatives, this remains true at syzygy.

\section{Universal reduced system}

Let
\[
\mu_1=\frac{m_1m_2}{m_1+m_2},\qquad
\mu_2=\frac{m_3(m_1+m_2)}{m_1+m_2+m_3}.
\]
Rotate so that $X=(R,0)$ and write $Y=(P,Q)$.  At zero angular momentum,
Routh reduction gives
\[
K_{red}=\frac{\mu_1\dot R^2+\mu_2(\dot P^2+\dot Q^2)}2
-\frac{\mu_2^2(P\dot Q-Q\dot P)^2}
{2[\mu_1R^2+\mu_2(P^2+Q^2)]}.
\]
The positive potential is
\begin{align*}
U={m_1m_2\over R}
&+{m_1m_3\over\sqrt{(P+{m_2R\over m_1+m_2})^2+Q^2}}\\
&+{m_2m_3\over\sqrt{(P-{m_1R\over m_1+m_2})^2+Q^2}}.
\end{align*}
The Pythagorean curve on the brake boundary is
\[
R=1,\qquad P={AB(B-A)\over A+B},\qquad Q=AB,
\qquad \dot R=\dot P=\dot Q=0.
\]

\section{Exact initial geometry}

The initial center of mass is the incenter because its barycentric weights are
the opposite side lengths.  With $I=\sum m_i|q_i-q_{CM}|^2$ and positive
potential magnitude $U$, direct calculation gives
\[
I_0=abc,\qquad
U_0={ab\over c}+{ac\over b}+{bc\over a}.
\]
Thus after normalization
\[
I_0=AB,\qquad U_0=AB+(AB)^{-1},\qquad H=-U_0.
\]
The configurational measure at fixed masses is
\[
\mu_0=U_0\sqrt{I_0}=(AB)^{3/2}+(AB)^{-1/2},
\]
and decreases strictly on the fundamental interval.  It is not invariant under
the simultaneous mass--length scaling used above.

For $D=r_{12}^2-r_{23}^2-r_{31}^2$, Newton's equations give
\[
D(0)=\dot D(0)=0,\qquad
\ddot D(0)=2\left(\frac1{AB}-AB(A+B)\right)>0.
\]
This is local only.  Converged ordinary numerical experiments show $D$ later
changes sign for $u=1/3$, so a global $D>0$ cone is not pursued.
An exact fourth-order calculation gives
\[
D^{(4)}(0)={P_{20}(u)\over
u^4(u-1)^4(u+1)^2(1+u^2)^4}.
\]
Exact Sturm arithmetic shows that $P_{20}$ has no zero on
$(0,83/200)$ and $P_{20}(0)=-1$.  Hence $D^{(4)}(0)<0$ throughout the
fundamental interval.  This uniform local sign still supplies no global cone.

\section{The real isosceles-right endpoint}

\begin{theorem}
At $u=\sqrt2-1$, the classical solution has no positive second brake and
reaches a binary or triple collision in finite time.
\end{theorem}
\begin{proof}
Here $m_1=m_2=m=1/\sqrt2$ and reflection symmetry persists.  Write the base
bodies as $(-x,y_b),(x,y_b)$ and the apex as $(0,y_a)$, with
$h=y_a-y_b$.  Then
\[
\ddot x=-\frac{m}{4x^2}-\frac{x}{(x^2+h^2)^{3/2}}<0.
\]
Starting with $x_0=1/2$ and $\dot x(0)=0$ gives $\dot x<0$, excluding a
second brake.  While $x>0$, one has
$\ddot x\leq-m/(4x_0^2)$, whence
\[
x(t)\leq x_0-\frac{m}{8x_0^2}t^2.
\]
Thus collision occurs no later than $\sqrt{8x_0^3/m}=2^{1/4}$.
\end{proof}

The endpoint is irrational, so this theorem does not settle an integer case.
Its perturbation is singular because the collision unfolds into a close
ordinary encounter when reflection symmetry is broken.

\section{The first skinny encounter}

Put $\epsilon=B$, $A=\sqrt{1-\epsilon^2}$, and $M=A+1$. For the tight-pair
and outer Jacobi vectors
\[
X=q_3-q_1,\qquad Y=q_2-\frac{Aq_1+q_3}{M},
\]
rotate the encounter frame so that $X(0)/\epsilon=1$ and scale
\[
X=\epsilon x,\qquad t=\frac{\epsilon^{3/2}}{\sqrt M}\tau.
\]
The exact inner equation is
\[
x_{\tau\tau}=-\frac{x}{|x|^3}
+\frac{\epsilon^3}{M}\left[
\Phi\left(y-\frac{A\epsilon}{M}x\right)
-\Phi\left(y+\frac{\epsilon}{M}x\right)\right],
\qquad \Phi(v)=\frac{v}{|v|^3}.
\]
The outer acceleration in $\tau$-time is $O(\epsilon^3)$ on a fixed
encounter interval. Introduce Levi--Civita variables
\[
x=z^2,\qquad d\tau=|z|^2ds.
\]
The augmented equations for $z$, $z_s$, the osculating Kepler energy, and the
outer state are real analytic through $z=0$. At $\epsilon=0$,
\[
z_0(s)=\cos(s/\sqrt2),\qquad s_c=\pi/\sqrt2.
\]
The first transverse parameter variation occurs at order five and satisfies
\[
v_{ss}+\frac12v=-\frac38\cos^5(s/\sqrt2),\qquad
v(0)=v_s(0)=0,
\]
so that
\[
v(s_c)=-\frac{15\pi}{128}.
\]

\begin{theorem}[First skinny miss]
There is $\epsilon_0>0$ such that for every real member with
$0<B=\epsilon<\epsilon_0$, the first tight-pair encounter is collision-free.
At closest passage,
\[
r_{13,\min}=\frac{225\pi^2}{16384}\epsilon^{11}(1+O(\epsilon)),
\qquad
h_{13}=-\frac{15\pi}{64}\epsilon^{11/2}
+O(\epsilon^{13/2}),
\]
where $h_{13}=X\times\dot X$ is specific pair angular momentum.
\end{theorem}
\begin{proof}
Analytic dependence gives
\[
\operatorname{Im}z(s_c,\epsilon)
=-\frac{15\pi}{128}\epsilon^5+O(\epsilon^6).
\]
Since $\partial_s\operatorname{Re}z_0(s_c)=-1/\sqrt2$, choose a fixed
neighborhood on which the real part remains strictly decreasing. Its unique
zero has the displayed nonzero imaginary part. Outside that neighborhood
$z_0$ is bounded away from zero up to a fixed outgoing section, while both
outer separations are uniformly bounded below. Thus no collision occurs.
Squaring the transverse displacement and converting the scaled angular
momentum to physical time gives the two coefficients.
\end{proof}

The first-encounter result alone does not prove skinny nonperiodicity. The binary remains bound
with specific energy $-M/\epsilon+O(\epsilon^3)$, and the first-passage outer
radial escape margin is $-2+O(\epsilon)$. The outer body falls back on an
order-one time scale, during which the binary executes
$O(\epsilon^{-3/2})$ cycles. Later sections control this train and prove
nonperiodicity on infinitely many phase windows, but not uniformly in phase.

\begin{lemma}[Terminal escape certificate]
For a prospective binary of total mass $M$ and an outer body of mass $m_c$,
let $r,\rho$ be the inner and outer Jacobi radii and
$e=|\dot x|^2/2-M/r$. Choose $\eta>0$, set $R=M/\eta$ and
$s_0=\rho_0-R$, and suppose
\[
s_0>0,\quad \dot\rho_0>0,\quad
\delta=\frac12\dot\rho_0^2-\frac{M+m_c}{s_0}>0,
\]
\[
e_0+\frac{m_c\sqrt{2MR}}
{\sqrt{2\delta}\,s_0^2}<-\eta.
\]
Then the future solution either ends in inner binary collision or the outer
body escapes with $\dot\rho\ge\sqrt{2\delta}$ forever. In particular there is
no later classical brake.
\end{lemma}
\begin{proof}
On the bootstrap region $e<-\eta$, one has
$r<R$ and $r|\dot x|<\sqrt{2MR}$. The Newton field derivative bound
$\|D(v/|v|^3)\|\le2/|v|^3$ gives
\[
|\dot e|\le\frac{2m_cr|\dot x|}{(\rho-r)^3}.
\]
Meanwhile
\[
J=\frac12\dot\rho^2-\frac{M+m_c}{\rho-R}
\]
is nondecreasing while $\dot\rho>0$. Hence
$\rho\ge\rho_0+\sqrt{2\delta}(t-t_0)$, and the total future tidal increase of
$e$ is at most the allowance in the hypothesis. Its strict margin closes the
bootstrap.
\end{proof}

\subsection{The late-plunge restricted limit}

The terminal lemma cannot be applied uniformly over the later binary phase.
Indeed, rescale near the outer plunge by
\[
X=\epsilon R,\qquad Y=\epsilon Z,\qquad
t=t_*+\epsilon^{3/2}\theta.
\]
On compact nonsingular sets the exact Jacobi equations tend to
\[
R_{\theta\theta}=-2\frac{R}{|R|^3},\qquad
Z_{\theta\theta}
=-\left[\Phi(Z+R/2)+\Phi(Z-R/2)\right].
\tag{LR}
\]
After regularizing the prescribed binary, this convergence extends through
its binary collisions provided the light particle is separate. The invariant
rectilinear subspace has
\[
R=(r,0),\quad Z=(0,z),\qquad
\ddot z=-\frac{2z}{(z^2+r^2/4)^{3/2}},
\]
where $r$ is the periodic collision--ejection binary with apocenter one.
The outer instantaneous energy obeys
\[
\frac d{d\theta}\left(
\frac12\dot z^2-\frac2{\sqrt{z^2+r^2/4}}\right)
=\frac{r\dot r}{2(z^2+r^2/4)^{3/2}}.
\]

\begin{theorem}[Restricted parabolic separatrix]
When the light particle crosses $z=0$ at binary apocenter, there is a launch
speed $v_*>0$ for which it has no light--heavy collision and
\[
z(\theta)\to+\infty,\qquad \dot z(\theta)\to0
\]
as $\theta\to+\infty$. Reflection and time reversal give a
parabolic-to-parabolic scattering orbit.
\end{theorem}
\begin{proof}
For positive $z$, $\ddot z<0$. Small launch speeds turn: with
$r=\cos^2\psi$ and $d\theta=\cos^2\psi\,d\psi$, the zero-speed variational
equation becomes, after $\zeta=\cos\psi\,w$,
\[
w_{\psi\psi}+(1+14\sec^2\psi)w=0.
\]
Sturm comparison forces a simple zero before the first binary collision.
Large launch speeds reach $z=1$ while retaining a speed for which
$\dot z^2/2-2/z>0$; this quantity is thereafter nondecreasing.
Let $\mathcal T$ be the nonempty open bounded set of speeds with a finite
turn and put $v_*=\sup\mathcal T$. The boundary solution cannot turn and
cannot have positive limiting speed, since either property is open. It cannot
remain bounded, because the periodic attraction would then force a turn.
\end{proof}

The binary collisions in (LR) are regularized features of an auxiliary limit,
not physical continuations of a positive-$\epsilon$ orbit. Nevertheless the
separatrix proves that a strict phase-uniform escape margin is impossible in
the limiting problem. A finite-$\epsilon$ proof must instead match the
incoming data and determine the separatrix splitting.

\subsection{Reference phase and finite-parameter splitting}

Put $A=\sqrt{1-\epsilon^2}$, $M=1+A$, $N=M+\epsilon$, and let
$\rho_0^2=2A^2/M$ be the exact initial outer Jacobi radius. Replacing the tight
pair by a point mass gives the exact reference clock
\[
\Phi_{\rm ref}
=\frac{\pi}{\epsilon^{3/2}}\sqrt{\frac MN}\,\rho_0^{3/2}
=\pi\epsilon^{-3/2}-\frac\pi4\epsilon^{-1/2}
-\frac{15\pi}{32}\epsilon^{1/2}+O(\epsilon^{3/2}).
\tag{RC}
\]
Moreover
$\Phi_{\rm ref}'\sim-(3\pi/2)\epsilon^{-5/2}$. Thus every open phase arc
has infinitely many reference preimage intervals accumulating at zero, each
containing rational Euclid parameters. The matching theorem below transfers
this reference statement to compact open windows of the true flow.

That long-cycle step can be proved without finite-eccentricity averaging. Fix
$0<\alpha<1/6$ and stop at physical outer radius
$\rho=\epsilon^\alpha$. Taylor cancellation in the outer Jacobi field gives a
monopole remainder $O(\epsilon^2\rho^{-4})$. Inner-energy and radial
comparison estimates keep $|X|\le2\epsilon$ and give
\[
t_\alpha-t_0(\epsilon^\alpha)=O(\epsilon^{2-3\alpha}).
\]
In LC variables the scaled binary forcing is
$O(\epsilon^{4-3\alpha})$ over regularized length
$O(\epsilon^{-3/2})$. Harmonic-oscillator variation and the exact LC clock
identity yield a regularized mean-anomaly error
$O(\epsilon^{1-3\alpha})$. Hence, unless a prior classical collision has
already ended the orbit, the renormalized incoming intercept satisfies
\[
\operatorname{dist}_{S^1}(\chi_\alpha,\Phi_{\rm ref})
=O(\epsilon^{1/2-3\alpha})=o(1).
\tag{PM}
\]
\begin{theorem}[Incoming-tail matching]
Fix a sufficiently large scaled incoming section $\Sigma^-_K$. As
$\epsilon\to0$, either a classical collision occurs before section reach or
the exact regularized state $S_\epsilon(K)$ satisfies
\[
\operatorname{dist}\!\left(
S_\epsilon(K),\Gamma^-_K(\Phi_{\rm ref}(\epsilon))\right)=o(1),
\tag{IT}
\]
where $\Gamma^-_K(\chi)$ is the restricted incoming parabolic state with
asymptotic mean-anomaly intercept $\chi$.
\end{theorem}
\begin{proof}
Put $Y_e=\epsilon^{\alpha-1}$ with $0<\alpha<1/6$. The exact cancellation of
the outer dipole leaves an $O(Y^{-4})$ remainder on the tail. If
\[
\mathcal H_N={1\over2}|Z_\theta|^2-{N\over Y},
\qquad \mathcal L=Z\times Z_\theta ,
\]
then the pre-plunge rates and a simultaneous energy--torque bootstrap give
\[
|\mathcal H_N-E_{\rm ref,\epsilon}|\le CY^{-3},\qquad
|\mathcal L|\le CY^{-3/2},\qquad
-Y_\theta\asymp Y^{-1/2}.
\tag{TB}
\]
These estimates are uniform for $K\le Y\le Y_e$.

Collision-uniform binary control uses exact LC first integrals rather than a
singular Delaunay chart. With normalized energy $E$ set
\[
\mathcal J=(E+1,L,K_{12}),\quad
L=z_xp_y-z_yp_x,\quad
K_{12}=p_xp_y-{E\over2}z_xz_y .
\]
On the radial LC circle,
\[
\det{\partial(L,K_{12})\over\partial(z_y,p_y)}=-{1\over2}.
\]
The three components are antipodal-invariant uniform normal coordinates, and
direct differentiation of the forced LC equations gives
$|\mathcal J_s|\le C\epsilon Y^{-3}$. Complete LC blocks have uniformly
comparable physical lengths; the two incomplete endpoint blocks are bounded
directly in LC time. Consequently
\[
|\mathcal J(Y)|\le
O(\epsilon^{5/2-3\alpha})+C\epsilon Y^{-3/2},
\]
and the accumulated binary-clock error is
\[
O(\epsilon^{1-3\alpha/2})
+O(\epsilon|\log\epsilon|)=o(1).
\tag{BC}
\]

Retain the exact finite-$\epsilon$ subtraction
\[
F_\epsilon(Y)=n_{\rm bin}
\bigl(t_{\rm mono}-t_0(\epsilon Y)\bigr)
\]
until $Y$ is fixed. The reciprocal-speed consequence of (TB) and (BC) is
\[
\operatorname{dist}_{S^1}\!\left(
\Lambda_\epsilon(Y)+F_\epsilon(Y),\chi_\epsilon\right)
\le CY^{-1/2}+o(1),
\tag{PG}
\]
where $\chi_\epsilon=\Phi_{\rm ref}(\epsilon)+o(1)$. The
$CY^{-1/2}$ term retains the restricted quadrupole flight correction.

For any sequence $\epsilon_n\to0$, LC compactness gives a diagonal limiting
restricted tail. The inherited angular bounds are
$Z_\perp=O(Y^{-1})$ and $Z'_\perp=O(Y^{-5/2})$. Reflection oddness factors
the exact transverse equation as
\[
\xi''+\left(2Y^{-3}+O(Y^{-5})\right)\xi=0.
\]
With $x=\log Y$ this is an $L^1$ perturbation of a constant system whose modes
are $Y$ and $Y^{1/2}$; the inherited decay forces $\xi\equiv0$. The limit is
therefore the rectilinear incoming parabolic orbit. Taking
$\epsilon_n\to0$ at fixed $Y$ in (PG), and only then $Y\to\infty$, identifies
its intercept as the limit of $\Phi_{\rm ref}(\epsilon_n)$. Uniqueness of the
incoming McGehee graph proves (IT).
\end{proof}

On the limiting symmetric orbit let $d^2=z^2+r^2/4$. Expansion of the exact
finite-parameter Jacobi field gives
\[
r''=-\frac2{r^2}-\epsilon\frac r{d^3}
+\frac{\epsilon^2}{2r^2}+O(\epsilon^3),
\]
\[
Z_y''=-\frac{2z}{d^3}-\epsilon\frac z{d^3}
+\epsilon^2\frac z{2d^3}+O(\epsilon^3),\qquad
Z_x''=\epsilon^2\frac{3r^3}{16d^5}+O(\epsilon^3).
\tag{FS}
\]
The centered order-$\epsilon$ energy-transfer variation cancels by parity;
longitudinal bending starts at order $\epsilon^2$. The first
symmetry-allowed splitting direction is binary phase. Its naive whole-line
Jacobi field is nevertheless gauge-degenerate: choosing the global
time-translation field $\zeta=z'$ makes the proposed integrand exactly
\[
\frac d{d\theta}\left(\frac{rr'}{2d^3}\right).
\]
Because $z'=O(|\theta|^{-1/3})$ at parabolic infinity, zero limiting data do
not eliminate this mode. A well-posed transversality theorem must instead fix
the parabolic epoch or compare incoming and outgoing parabolic curves
$V_u,V_s$ on $z=0$. Reversibility gives $V_s(\phi)=V_u(-\phi)$, so the target
is $2V_u'(0)\ne0$. Ordinary finite-cutoff calculations support this with a
slope difference near $-0.90623$, but no analytic or validated result is
claimed.

There is a finite sufficient certificate for this transversality. Let
$h''=ah$, $h(0)=v_*$, $h'(0)=0$, with
$a=(4z^2-r^2/2)/(z^2+r^2/4)^{5/2}$. Fixed-softening comparison and an explicit
early escape estimate give
\[
\sqrt8<v_*<4.
\]
If $W=hz''-h'z'$, then
$W'=hbr'$ with $b=3zr/[2(z^2+r^2/4)^{5/2}]$. During the first binary
contraction this proves $(h/z')'>0$ as long as $h>0$, and hence rules out a
first zero of $h$. A force bound also gives
\[
z(\pi/8)>
\frac{\pi\sqrt2}{4}-\frac{2\pi^2}{27\sqrt3}>\frac1{2\sqrt2},
\]
so $a>0$ thereafter. Thus $h'(\pi/4)>0$ forces $h$ to grow forever.

This finite sign is now rigorous. Put $q=h/v$, $w=z'$, $p=q'$, and use
eccentric anomaly $r=\cos^2\psi$, $d\theta=r\,d\psi$. The regular compact
system is
\[
z_\psi=rw,\quad w_\psi=-{2zr\over d^3},\quad
q_\psi=rp,\quad
p_\psi=r{4z^2-r^2/2\over d^5}q,
\tag{CJ}
\]
with $(z,w,q,p)(0)=(0,v,1,0)$ and terminal point $\psi=\pi/2$.

\begin{theorem}[Computer-assisted finite Jacobi sign]
For every $v\in[14/5,4]$, the solution of \emph{(CJ)} exists through
$\psi=\pi/2$ and
\[
p(\pi/2)>\frac1{125}.
\]
In particular, the parabolic-separatrix field satisfies
\[
h'(\pi/4)>\frac{14}{625}>0.
\]
\end{theorem}
\begin{proof}[Certificate description]
CAPD 6.1.0 native outward-rounded intervals were used at the pinned commit
\texttt{731079217a9254ea2948d742df2b170895effe7f}. The interval
$[14/5,4]$ was divided into 256 overlapping slabs and each slab was propagated
independently by an order-20 interval Taylor solver with a doubleton set.
Every terminal interval had lower endpoint greater than $1/125$. The complete
verifier prints exact hexadecimal binary64 endpoints; the dependency check,
concise certificate, and symbolic regression test are included in the
repository. Since $14/5<\sqrt8<v_*<4$ and $h=v_*q$, the stated corollary
follows.
\end{proof}

\begin{theorem}[Restricted parabolic transversality]
The incoming and outgoing parabolic curves of the rectilinear equal-heavy-mass
restricted problem cross transversely at the reversible apocenter-centered
orbit.
\end{theorem}
\begin{proof}
The collision-regularized stable-manifold implication can be checked directly.
Compactify infinity by $z=2/x^2$. In eccentric anomaly the field becomes
\[
x_\psi=-{r\over4}x^3w,\qquad
w_\psi=-{r\over2}x^4(1+r^2x^4/16)^{-3/2}.
\]
It is analytic at $(x,w)=(0,0)$ and through $r=0$. Since
$\int_0^\pi r\,d\psi=\pi/2$, its time-$\pi$ map has, after
$X=x$, $Y=w-\sqrt2x$, the form
\[
X_1=X-\frac\pi8X^3(Y+\sqrt2X)+O_7,\qquad
Y_1=Y+{\pi\sqrt2\over8}X^3Y+O_7.
\tag{PM}
\]
Before this shear, $y=w/\sqrt2$ puts the exact map in the factored form
\[
x_1=x-kx^3(y+R_1),\qquad y_1=y-kx^3(x+R_2),
\quad k={\pi\sqrt2\over8},
\]
where analyticity of the exact flow and the $x^3,x^4$ factors in the vector
field make $R_1,R_2$ analytic of order at least four (write $x=x_0u$ to
factor the exact flow by $x_0^3$ and $x_0^4$).
The degree-four term obeys $p_1(X,0)<0$, $p_2(X,0)=0$, and
$\partial_Yp_2(X,0)>0$ for $X>0$. McGehee's theorem
\cite{McGehee1973} therefore gives the fixed-phase parabolic stable curve
$Y=\varphi(X)$ with $\varphi(0)=\varphi'(0)=0$, analytic for $X>0$.
McGehee's graph construction supplies an arbitrarily small Lipschitz constant
on a narrowed sector, hence $|\varphi'|$ is correspondingly small for $X>0$.

Restricting (PM) to this graph gives, for positive constants $c_i$ and small
$X$,
\[
c_1X^4\le X-F(X)\le c_2X^4,\qquad
0<F'(X)\le1-c_3X^3.
\]
Thus $X_n^{-3}\asymp n$, $\sum X_n^3=\infty$, and every stable-curve tangent
$\delta X_n=(F^n)'(X_0)\delta X_0$ tends to zero.

Take both section speeds positive in the direction from $z<0$ to $z>0$.
The reversor $\widetilde z(t)=-z(-t)$ then gives
$V_s(\phi)=V_u(-\phi)$. Tangency at zero would force both slopes to vanish.
In extended variables $(\delta z,\delta w,\delta\phi)$, the global time-shift
and pinned stable-phase tangents are explicitly
\[
T_{\rm time}=(v_*,0,1),\qquad T_{\rm pin}=(0,V_s'(0),1)=(0,0,1).
\]
Their difference is the fixed-phase stable tangent with $z$-component
$h=z'-\eta_{\rm pin}$. The
validated theorem and $a>0$ imply
$h(\theta)\ge h(\pi/4)+(14/625)(\theta-\pi/4)$. But
\[
\delta X_n=-{X_n^3\over4}h(\theta_n),\qquad
\theta_n=\theta_0+n\pi/2,
\]
so $X_n^{-3}\asymp n$ makes $\liminf|\delta X_n|>0$, contradicting tangent
contraction.
\end{proof}

\begin{theorem}[Restricted phase windows]
There are one-sided open binary-phase intervals adjacent to the centered
separatrix such that the incoming parabolic orbit escapes hyperbolically on
one interval and turns at finite positive $z$ on the other.
\end{theorem}
\begin{proof}
The analytic section difference
$\Delta(\phi)=V_u(\phi)-V_s(\phi)$ is odd and has
$\Delta'(0)\ne0$. It therefore has a strict linear sign on the two sides of
zero. McGehee's stable curve separates the local hyperbolic and returning
sectors. On the returning side $z''<0$ for $z>0$; any trajectory that neither
turns nor escapes with positive speed would be another parabolic point on the
unique stable curve. Thus it turns in finite time.
\end{proof}

The finite-turn side contains a singular sequence that prevents a uniform
restricted no-brake argument.

\begin{theorem}[Restricted turn resonances]
For every sufficiently large integer $n$, there is a finite-turn phase
$\phi_n\to0$ whose first outer turn is simultaneous with the $n$th heavy-binary
apocenter. It is a nonsingular brake state of the collision-regularized
restricted flow. If $Z_{{\rm t},n}$ and $T_n$ are its turn height and time,
then
\[
Z_{{\rm t},n}=(2n)^{2/3}(1+o(1)),\qquad
T_n={\pi n\over2}+o(1).
\]
\end{theorem}
\begin{proof}
At center phase $\phi$, the binary mean anomaly is
$\Lambda(\theta)=\phi+4\theta$. The first-turn time $T(\phi)$ is continuous
on the finite-turn side and tends to infinity at the transverse parabolic
boundary; otherwise finite-time convergence would give a turn on the
parabolic separatrix. Thus $\phi+4T(\phi)$ crosses $2\pi n$ for every large
$n$. For $Z=Z_{\rm t}(\phi)$ and $w=z'>0$, the exact comparison identity
\[
{d\over dz}\left({w^2\over2}-{2\over z}\right)
={2\over z^2}\left[1-\left(1+{r^2\over4z^2}\right)^{-3/2}\right]
\]
gives $T=(\pi/4)Z^{3/2}(1+o(1))$. Combining this with
$\phi_n+4T_n=2\pi n$ proves the asymptotics.
\end{proof}

Intervening regularized heavy-heavy collisions mean these restricted brake
states are not classical periodic counterexamples. For finite $B$, they form
an infinite resonance sequence at which the full velocity residual must be
split.

At a fixed resonance this splitting has an exact local form. If
$a_n=z_{\theta\theta}(T_n)<0$, $b_n=\partial_\phi z_\theta(T_n)$, and
$\mathcal A(\phi)=\phi+4T(\phi)$, the Jacobian of the two Jacobi radial-rate
conditions with respect to phase and time is
\[
D_n=Z_n(2b_n-a_n/2)
=-{Z_na_n\over2}\mathcal A'(\phi_n).
\]
Thus radial continuation by the implicit-function theorem is equivalent to
simplicity of the turn-phase root. Simplicity is not required for local
exclusion: uniform convergence of the divided transverse velocity makes any
sequence of finite-$B$ brakes converging to a restricted brake satisfy
$0=Z'_{B,x}/B\to C_n$. Its first coefficient is
$C_n/Z_n$, where
\[
C_n=-{3\over4Z_n}\int_{-\infty}^{T_n}{r^2zp\over d^5}\,d\theta,\qquad
p''={r^2-2z^2\over d^5}p,\quad p-z=O(|z|^{-1})
\]
on the incoming tail. The first-order incoming theorem is now proved in
smooth divided Levi--Civita normal variables. On the centered parabolic
separatrix let $p_-$ have the same incoming normalization and define
\[
\mathcal W_\infty=\lim_{\theta\to+\infty}
  (zp_-'-z'p_-).
\]
The exact identity
\[
{d\over d\theta}(zp_-'-z'p_-)
={3r^2zp_-\over2d^5}
\]
makes this limit finite. Phase-uniform large-turn comparison, applied to
$q=p/z$ and its Wronskian, proves
\[
\boxed{Z_nC_n\longrightarrow-\mathcal W_\infty/2.}
\]
Thus one scalar inequality $\mathcal W_\infty\ne0$ implies $C_n\ne0$ at
every sufficiently late restricted resonance, uniformly over multiple roots.
Ordinary finite-cutoff integrations give
$\mathcal W_\infty\approx0.4457$. A finite CAPD enclosure, joined to an
analytic outgoing-Jost tail bound, proves
\[
k_+(0)>7/20,\qquad k_+'(0)>3/5,
\qquad {11\over25}<\mathcal W_\infty=2k_+(0)k_+'(0)<{9\over20}.
\]
Consequently $C_n<0$ at every sufficiently late restricted resonance. The
first-order tail theorem then proves that every fixed such resonance has a
punctured finite-$B$ neighborhood containing no full labelled brake. This
fixed-resonance conclusion does not require radial simplicity. It is not
uniform as $n\to\infty$, because $C_n\asymp Z_n^{-1}\to0$, and it does not
exclude a brake after later outer turns.

The joint scale is controlled by outer angular momentum. The outgoing restricted
field has
\[
p_- = \gamma z-\mathcal W_\infty\sqrt z+o(\sqrt z).
\]
On the macroscopic radial Kepler arc
$s=R\sin^2\psi$, the collision-normalized transverse field
$x=\sqrt R\sin\psi\cos\psi\sim\sqrt s$ solves $x''=-2x/s^3$ and obeys
$\dot x(\pi/2)=-1/R$. More invariantly, exact torque factorization and a
collision-safe outgoing LC bootstrap prove, uniformly in first-turn height,
\[
{Y\times\dot Y\over B^{3/2}}(t_{\rm turn})
\longrightarrow{\mathcal W_\infty\over2}>{21\over100}.
\]
Hence a full labelled brake is impossible at the first outer turn throughout
the accumulating skinny parabolic layer. The same tail estimate carries this
positive normalized angular momentum back to a fixed large section on the
inward leg. The next compact central encounter, and hence a brake after a
later turn, remains uncontrolled.

One endpoint of that compact encounter map is already signed. Let
$(R_+,S_+)$ be the outgoing transverse Jost basis with
$W(R_+,S_+)=-1$. Evenness and Wronskian preservation give the connection
matrix
\[
 \begin{pmatrix}
 \gamma&(1-\gamma^2)/\mathcal W_\infty\\
 -\mathcal W_\infty&\gamma
 \end{pmatrix}.
\]
The reflected returning field therefore has centered-parabolic outgoing
Wronskian
\[
 \mathcal C_{2,\mathrm{par}}=-2\gamma\mathcal W_\infty.
\]
The extended pinned interval certificate proves
$-1/100<\gamma<-1/250$, hence
$\mathcal C_{2,\mathrm{par}}>21/6250$. Thus a second-turn zero cannot emerge
from the centered parabolic endpoint. The compact captured interior and its
triple-collision transition remain open. On the equilateral restricted
triple-collision ray the transverse coefficient is exactly
$-1/(18\sigma^2)$, with indicial exponents $(3\pm\sqrt7)/6$; hence that
endpoint requires fractional-power McGehee matching. More precisely, its
remaining scalar target is
\[
 (\pi-\phi)^{(\sqrt7-1)/6}\mathcal C_2(\phi)
 \longrightarrow 4^{(\sqrt7-1)/6}A_-K_->0,
\]
where $A_-$ is the slow collision-mode coefficient of the returned Jost
field and $K_-$ is its universal first-turn Wronskian transfer.

One part of this endpoint problem is already analytic. In logarithmic time
$\tau=-\log(1-s)$ and shape coordinate $y=Z/R$, the limiting base satisfies
\[
 y_{\tau\tau}-{1\over3}y_\tau
 +{2\over9}y\bigl[(y^2+1/4)^{-3/2}-1\bigr]=0.
\]
Its increasing unstable branch from $y=-\sqrt3/2$ is unique modulo
$\tau$-translation, crosses $y=0$ with
$Z_s>9^{1/3}\sqrt{5/18}$, and obeys
$e^{-2\tau/3}y\to A_\infty>0$.  The last assertion follows from the
strictly decreasing positive factor
$e^{-2\tau/3}(y_\tau+y/3)$ and a tail contradiction. Thus the candidate
universal light orbit stays separated from the prescribed binary collision.
This does not yet prove that the finite-phase family selects that branch. The
two scalar signs $K_->0$ and $A_->0$ are settled below.

There is also an analytic sign on the incoming half of the canonical slow
transverse field. If
$Q(y)=(1-2y^2)/(9(y^2+1/4)^{5/2})$ and
$r=p_\tau/p$, then $r'=Q-r-r^2$ and $r\to-\alpha_-$ at the
equilateral end. Tangent-line bounds for $Q$, together with
$0<y_\tau\le y+\sqrt3/2$, make
$g=-\alpha_-+(7/100)(y+\sqrt3/2)$ a strict lower barrier. Hence
\[
 p(0)>0,\qquad {p_s(0)\over p(0)}
 \ge-\alpha_-+{7\sqrt3\over200}>0.
\]
The passage through the prescribed binary collision and the subsequent
short segment to the first turn can then be certified on a deliberately
larger box. The energy identity gives
$1/2<y_\tau(0)<3/4$, while the Riccati bound gives
$p_s(0)/p(0)>1/1000$. A pinned CAPD proof covers the center-speed interval in
256 dyadic slabs, crosses the binary collision in the analytic coordinate
$\lambda=\operatorname{sgn}(1-s)|1-s|^{1/3}$, and proves positive height and
positive transverse derivative at the unique first turn. Therefore
\[
 K_->0.
\]
The global coefficient is also positive, as proved next. The later
transversality and shifted-Fuchsian argument then construct the selected local
finite-phase branch.

The latter sign is nevertheless finite-dimensional. On the last binary
infall, use the monotone separation $r$ as independent variable and put
$y=z/r$. The exact equations are
\[
 2r^2(1-r)y_{rr}+r(3-4r)y_r
 +y[(y^2+1/4)^{-3/2}-1]=0,
\]
\[
 2r^2(1-r)p_{rr}-rp_r
 -{1-2y^2\over2(y^2+1/4)^{5/2}}p=0.
\]
At equilateral collision their admissible shape exponent is
$(-1+\sqrt{19})/4$, while the transverse exponents are
$\beta_\pm=(3\pm\sqrt7)/4$. Hence
\[
 \widehat A_-=\lim_{r\downarrow0}r^{-\beta_-}
 {\beta_+p-rp_r\over\beta_+-\beta_-}
\]
has the same sign as $A_-$. This reduces the global sign to one
collision-stable/parabolic-manifold intersection and one scalar projection.
A scalar comparison with the exact equilateral orbit shows that a branch with
negative shape-stable coefficient remains in the cone
$z<-(\sqrt3/2)r$ throughout the final infall. The global certificate below
places every candidate parabolic connection in such a negative interval.

This match can be made square without adding a hidden dynamical parameter.
On the regular section $r=1/2$, let $U(\chi)$ be the incoming parabolic base
curve, $C(a)$ the collision-stable base curve, $J(\chi)$ the returned Jost
state, and $P_\pm(a)$ the canonical collision modes. The four residuals are
\[
 C(a)-U(\chi)=0,
 \qquad A_-P_-(a)+A_+P_+(a)-J(\chi)=0.
\]
Their Jacobian is block lower triangular. In physical time the exact
collision-mode Wronskian is
\[
 W(P_-,P_+)=-\sqrt7,
\]
so its determinant is $-\sqrt7$ times the oriented determinant of the two
base curves. Thus only the base intersection requires a transversality
certificate, after which
\[
 A_-={W(J,P_+)\over-\sqrt7}.
\]
At incoming infinity, $z=-2/x^2$ and the regular fiber variables
$P=x^2p$, $\Omega=z\dot p-\dot zp$ tend directly to
$(2\gamma,-\mathcal W_\infty)$ and obey an analytic system. Hence both
singular ends attach to one compact interval-Newton problem. A backward
finite-cutoff diagnostic gives $a\approx-0.24695$ and
$\widehat A_-\approx0.31>0$.

\begin{theorem}[Global triple-endpoint coefficient]
There is exactly one collision-stable/incoming-parabolic connection in the amplitude bracket
\[
 -0.24696\le a\le-0.24694
\]
and it satisfies $A_->0$.
\end{theorem}
\begin{proof}
Analytic Fuchsian fixed-point estimates attach the full collision-stable
family and its canonical fast mode at $r=2^{-32}$. A pinned interval
propagation carries the whole amplitude box to a large outgoing section and
proves positive velocity along every accepted step. At the left endpoint the
monopole energy $H_K=w^2/2-2/Z$ is positive; at the right endpoint
$H_K+1/(4Z^3)<0$. The exact tail inequality
\[
 0\le H_K(\infty)-H_K(Z)\le {1\over4Z^3}
\]
and connectedness therefore supply a parabolic member, without a uniqueness
or transversality assumption.

The same propagation proves $Z>r/(2\sqrt2)$ along the whole candidate box;
the analytic collision cone and the large-$Z$ tail cover the two omitted
ends. Hence $f_Z>0$ for the scalar light equation. For two amplitudes ordered
by $b=-a$, their positive position and velocity differences obey
$\Delta Z''=c(t)\Delta Z$ with $c>0$. The velocity difference is strictly
increasing, so two different amplitudes cannot both have velocity tending to
zero at infinity. This proves uniqueness within the bracket and orders the
turn and escape sides.

On every parabolic member, a Volterra estimate in $(P,\Omega)$ attaches the
exact returned Jost data
$(2\gamma,-\mathcal W_\infty)$ from infinity. Combining those boxes with the
compact propagation gives the uniform rational margin
\[
 W(J,P_+)<-\frac12.
\]
Since $W(P_-,P_+)=-\sqrt7$, the exact projector gives
\[
 A_-={W(J,P_+)\over-\sqrt7}>0.
\]
The collision attachment is analytic; the compact enclosures use CAPD 6.1.0
at pinned commit \texttt{731079217a9254ea2948d742df2b170895effe7f}.
\end{proof}

Together with $K_->0$, this proves that the proposed limiting coefficient
$4^{(\sqrt7-1)/6}A_-K_-$ is positive. It does not by itself prove the limit:
one must still show that the finite-phase captured family selects this base
connection and its normalized transverse field.

That singular local implication has an exact shifted-Fuchsian reduction. If
$\rho$ is the binary separation at center crossing and
$\xi=-\log(r/\rho)$, then
\[
 2(1-\rho e^{-\xi})y_{\xi\xi}
 +(-1+2\rho e^{-\xi})y_\xi
 +y[(y^2+1/4)^{-3/2}-1]=0.
\]
The translated field converges to the universal hyperbolic saddle. A
parameter-dependent inclination estimate selects its increasing unstable
branch, while the fibered exchange estimate suppresses the fast mode by
$\rho^{\sqrt7/2}$. Thus fixed-section convergence to the certified endpoint
implies the full base-and-field asymptotic above. The remaining issue is the
compact localization of any remotely continued captured component; the
singular passage itself introduces no further sign ambiguity.

The certified connection is in fact a transverse base-curve intersection.
Differentiating the collision contraction in the fixed terminal-collision
gauge gives a tangent $J$ with $J,J_s>0$. The validated cone makes
$J_{ss}=f_ZJ>0$, so its velocity component stays bounded away from zero.
By contrast, the differentiable McGehee fixed-phase parabolic graph has
contracting tangents, whose velocity components tend to zero at infinity.
The section tangents therefore cannot be collinear.

It follows that one side of the incoming parabolic curve exits the translated
saddle along the increasing universal peel. This produces a genuine local
captured branch with center phase tending to $\pi$. On that branch the full
fibered exchange theorem gives
\[
 (\pi-\phi)^{(\sqrt7-1)/6}\mathcal C_2(\phi)
 \longrightarrow4^{(\sqrt7-1)/6}A_-K_->0.
\]
Thus the restricted second-turn coefficient is positive on a punctured
one-sided neighborhood of the certified triple endpoint. This is a
collision-regularized restricted theorem. A finite-positive-mass wedge can now
be transferred as follows.

Let $\omega_0(B),\omega_1(B)\to0$ be respectively the fixed-section full-state
and divided-transverse matching moduli supplied by the incoming-tail theorem.
Put
\[
 \lambda_s={1+\sqrt{19}\over6},\qquad
 \lambda_t={1+\sqrt7\over6},\qquad \lambda_s>\lambda_t.
\]
For an actual Pythagorean trajectory whose selected intercept
$\Phi_{\rm ref}(B)$ lies on the certified local restricted branch, let
$\varepsilon=(\pi-\phi)/4$ be the comparison center-to-collision gap.

\begin{theorem}[Finite-mass triple-endpoint wedge]
If $B_j\to0$, $\varepsilon_j\to0$, and
\[
 \omega_0(B_j)+B_j=o(\varepsilon_j^{\lambda_s}),
\]
then either the finite-$B_j$ classical trajectory has a collision during the
near-triple passage, or it has no full labelled brake near the continued
restricted second turn. In the collision-free alternative, with
$\eta_j=B_j\varepsilon_j^{-\lambda_t}$,
\[
 {1\over\eta_j}(\mathcal Z_{j,x},\mathcal Z_{j,x,s})
 \longrightarrow -{A_-\over2}(P_-,P_{-,s})
\]
through the heavy-binary encounter and at the simple universal turn.
\end{theorem}
\begin{proof}
Homogeneity leaves the exact finite-$B$ Jacobi field unchanged under
$R,Z=\varepsilon^{2/3}(\mathcal R,\mathcal Z)$ and
$\theta=\theta_c+\varepsilon s$. If $h$ is the signed unstable distance from
the certified collision-stable graph, transversality gives
$h\asymp\varepsilon^{\lambda_s}$, while the actual transverse displacement
relative to the inner size is $\eta=B\varepsilon^{-\lambda_t}$.

The full homogeneous McGehee system, after zero-angular-momentum rotation
reduction, has reduced log-size spectrum
\[
 -{\sqrt{19}-1\over4},\quad {1+\sqrt{19}\over4},\quad
 1-{3-\sqrt7\over4},\quad 1-{3+\sqrt7\over4}.
\]
A simultaneous longitudinal/transverse graph-transform bootstrap bounds the
translated error by
\[
 O\!\left({\omega_0+B\over h}+B|\log h|+\eta+\omega_1\right)=o(1).
\]
It also proves that the actual and restricted center scales have ratio
$1+o(1)$. At the heavy-binary encounter, reflection and Hadamard division of
the smooth Levi--Civita field leave a direct source of size
$B/\eta=\varepsilon^{\lambda_t}\to0$. Thus the divided outer state passes
through the regularized block and converges in physical $C^1$ near the
separated simple turn. Since $p=-2\xi$, its limit is the one displayed.
Finally $A_->0$ and $K_-=Z_*(s_t)P_{-,s}(s_t)>0$, so the limiting transverse
velocity is nonzero. A true finite-$B$ collision instead terminates the
classical solution. Full details, including the fixed-inertial reconstruction,
are in \texttt{docs/FINITE\_B\_TRIPLE\_ENDPOINT.md}.
\end{proof}

In particular, with
\[
 \Delta(B)=(\omega_0(B)+B)^{3/(1+\sqrt{19})},
\]
all sufficiently small finite masses are excluded from a brake on the local
branch for $\Delta(B)\le\varepsilon\le\varepsilon_0$. The complementary
boundary layer $0<\varepsilon<\Delta(B)$ remains open.

There is nevertheless no brake *inside* that boundary layer's near-triple
tube. Indeed the heavy pair has physical separation $X=BR$, while at every
full brake energy conservation requires $U=U_0=AB+(AB)^{-1}$. Positivity of
the other pair terms gives the exact strict condition
\[
 |R|>{A\over BU_0}
 ={1-B^2\over1+B^2-B^4}=1-2B^2+O(B^4).
\]
Thus every state with $|R|=o(1)$ is separated from the brake boundary. The
unresolved boundary layer concerns the signed outgoing data and possible later
motion after the heavy pair re-expands, not a brake during the close passage.

\begin{theorem}[Joint endpoint reduction]
Let $B_j\downarrow0$ be collision-free finite-mass states whose selected
restricted intercepts approach the certified triple endpoint. There are
intrinsic longitudinal and reflection-odd unstable amplitudes $H_B,T_B$ for
which
\[
 {T_B\over B}\longrightarrow-{\widehat A_-\over2}\ne0.
\]
Put
\[
 \mu={1+\sqrt{19}\over4},\qquad
 \tau={1+\sqrt7\over4},\qquad
 \rho_T=|T_B|^{1/\tau},\qquad
 \kappa_B={H_B\over|T_B|^{\mu/\tau}}.
\]
If $\kappa_{B_j}\to\kappa\in\mathbb R$, scaling size by $\rho_T$ and time by
$\rho_T^{3/2}$ gives the one-parameter planar massless restricted limit
\[
 {\cal R}_{ss}=-2\Phi({\cal R}),\qquad
 {\cal Z}_{ss}=-\Phi({\cal Z}+{\cal R}/2)
               -\Phi({\cal Z}-{\cal R}/2).
\]
If $\kappa_{B_j}\to\pm\infty$, longitudinal scaling instead selects the
corresponding rectilinear exit. A genuine finite-$B$ collision is always the
classical-termination alternative.
\end{theorem}

\begin{proof}
Use the parity-adapted nonlinear unstable amplitudes of the persisted
McGehee rest point. In the auxiliary family with equal heavy masses
$(1,B,1)$, heavy exchange and reflection split the stable foliation and
spectral projectors into exact even and odd blocks. Since
$A(B)=\sqrt{1-B^2}=1+O(B^2)$, the Pythagorean field differs from this
auxiliary field by $O(B^2)$ in $C^2$. Subtracting the moving stable graph
therefore does not alter the divided first-order odd coefficient, and the
proved Fuchsian matching gives $T_B/B\to-\widehat A_-/2$.

After translations, SO(2), log-size/time, and fixed energy have been removed,
the unstable space has exactly these two scalar modes. The longitudinal
stable mode, fast odd mode, and energy/clock variables lie in the stable or
triangular block; no planar angle remains after zero-angular-momentum
reconstruction.

The two unstable exponents satisfy
$1<\mu/\tau<2$. Thus $H\mapsto H+O(T^2)$ changes $\kappa_B$ by $o(1)$, while
$B|\log B|\to0$ absorbs finite-$B$ exponent drift over the saddle dwell.
The exponential dichotomy gives, on every fixed translated interval, the
uniform error
\[
 O_L\!\left(
 |\kappa_B-\kappa|+B|\log|T_B||+B
 +|T_B|^{2-\mu/\tau}+e^{-c|\log|T_B||}\right)+o_L(1).
\]
Translation by $-\log\rho_T$ and Newtonian homogeneity therefore give the
displayed one-parameter limit. Across a separated
limiting heavy-binary collision the convergence is only in the common
Levi--Civita variables and clock; an exact collision of a finite member is
not continued.
\end{proof}

This theorem removes the arbitrary two-parameter character of the shrinking
boundary layer, but it does not yet exclude a later brake. The remaining
local task is a global classification and a nonvanishing scattering theorem
for the single real family indexed by $\kappa$.

\begin{lemma}[Planar shape identities]
On a zero-energy radial-binary half put
\[
 R=9^{1/3}t^{2/3}e_x,\qquad
 Z=9^{1/3}t^{2/3}w(\zeta),\qquad \zeta=\log t.
\]
Then
\[
 w_{\zeta\zeta}+{1\over3}w_\zeta=\nabla W(w),
\]
where
\[
 W={1\over9}\left(
 |w|^2+{|w+e_x/2|}^{-1}+{|w-e_x/2|}^{-1}\right).
\]
If $\mathcal E=|w_\zeta|^2/2-W$ and
$\Lambda=w\times w_\zeta$, then
\[
 \mathcal E_\zeta=-{|w_\zeta|^2\over3},\qquad
 \Lambda_\zeta+{\Lambda\over3}=-xyK(x,y),\quad K>0.
\]
The sharp global bound is $W\ge11/36$, with equality only at the two
equilateral shapes. Hence a nonhomothetic equilateral-stable orbit has
$w_\zeta\ne0$ at every finite point and has no compact collision-free
backward-captured component.
Moreover, on $|y|\ge\sqrt3/2$,
\[
 W_{xx}\ge {1\over9}\left({7\over4}-{8\over3\sqrt3}\right)>0.
\]
\end{lemma}

\begin{proof}
The first two identities follow by direct substitution and scalar/cross
products. If $d_\pm=|w\pm e_x/2|$, then
\[
 9W=\sum_\pm\left({d_\pm^2\over2}+{1\over d_\pm}\right)-{1\over4}
 \ge {11\over4},
\]
since
$d^2/2+d^{-1}-3/2=(d-1)^2(d+2)/(2d)$. The strict energy identity excludes a
finite shape rest. A compact backward tail would have an invariant
$\alpha$-limit set on which the energy is constant, hence
$w_\zeta=0$ and $\nabla W=0$; its energy would be at most $-11/36$, contrary
to the strict stable-tail energy.

For the last inequality, set $Y=y^2$ and
$g_Y(q)=(Y-2q)/(Y+q)^{5/2}$. Then
\[
 9W_{xx}=2-g_Y((x+1/2)^2)-g_Y((x-1/2)^2).
\]
One squared argument is at least $1/4$; monotonicity of $g_Y$ gives an upper
bound
$Y^{-3/2}+(Y-1/2)/(Y+1/4)^{5/2}$, whose maximum for $Y\ge3/4$ is
$8/(3\sqrt3)+1/4$.
\end{proof}

The angular identity fixes a nonzero sign on every stable tail that remains
in the outer strip. It is not yet global: at $\kappa=0$ the stable manifold
bends into the inner strip with the exact coefficient
$(2\sqrt{21}-5\sqrt3)/4>0$. Thus the next problem is a finite transition
classification, not an invariant-strip assertion. On the incoming half
$t=t_c-\theta$, so the physical angular-momentum sign is opposite the
$\zeta$-oriented sign above.

For the collision shoot we use the exact quartic stable jet. With
$p=Te^{-a_T\zeta}$ and $\nu=He^{-a_L\zeta}$ it has the normalized form
\[
 x=p+Ap\nu+Bp^3+Cp\nu^2+O_4,\qquad
 q=\nu+Kp^2+D\nu^2+Ep^2\nu+F\nu^3+O_4,
\]
Its degree-four correction is
\[
 x_{[4]}=Gp^3\nu+Hp\nu^3,\qquad
 q_{[4]}=Ip^4+Jp^2\nu^2+L\nu^4.
\]
All twelve coefficients are explicit elements of
$\mathbb Q(\sqrt3,\sqrt7,\sqrt{19})$. Direct substitution kills the full
residual through total degree four.

\begin{theorem}[Stable-tail enclosure]
Let $P_4$ be this quartic parameterization and equip vector power series with
the weighted coefficient norm
\[
 \|F\|_{r_T,r_L}=\sum_{i,j}(|F^x_{ij}|+|F^q_{ij}|)r_T^ir_L^j.
\]
On $|p|\le1/200$, $|\nu|\le1/2500$, there is a unique normalized analytic
stable parameterization $P=P_4+E$, absolutely convergent on the closed
polydisc, such that
\[
 \|E\|_{1/200,1/2500}< {1\over125000000}.
\]
On the half polydisc,
\[
 \|E\|<{1\over4000000000},\qquad
 \|\mathcal DE\|<{1\over800000000},
\]
and
\[
 \|\partial_\kappa E\|<10^{-9},\qquad
 \|\partial_\kappa\mathcal DE\|<5\cdot10^{-9}.
\]
These half-radius estimates apply to the real collision-shooting tail
$p=-e^{-a_T\zeta}$, $\nu=\kappa e^{-a_L\zeta}$ for
$5/4\le\kappa\le13/10$ and $\zeta\ge10$.
\end{theorem}

\begin{proof}
The homological inverse on degrees at least two has norm below $30/17$ because
its smallest divisor is
$(5+2\sqrt7)/18>17/30$. Exact algebraic coefficient bounds on $P_4$ and an
eightyfold Cauchy dilation give residual norm $<13/80^5$. On the base-radius
ball $R=271/50000$, a rational majorant gives
$\|D^2\nabla W\|<9$, hence contraction factor
$(30/17)9R<1/10$. The correction is consequently below
$13/1671168000<1/125000000$. Since it begins in degree five, restriction to
half radii and coefficient differentiation give the displayed state and
parameter-tangent bounds. Exact Taylor lower bounds for the two exponentials
put the stated real tail inside the half polydisc.
\end{proof}

\begin{lemma}[Forced light-collision chart]
Let $\epsilon=\pm1$ select the heavy primary at $\epsilon R/2$ and put
$q=Z-\epsilon R/2$. On either heavy-binary half let
$t=|\theta-t_c|$ and $\eta=dt/d\theta=\pm1$, where $\theta$ is physical
time. Then derivatives of order two in $t$ and $\theta$ agree, and
\[
 q_{tt}=-\Phi(q)+G_\epsilon(q,t),\qquad
 G_\epsilon=\epsilon\Phi(R)-\Phi(q+\epsilon R).
\]
With complex Levi--Civita variables
\[
 q=u^2,\qquad dt=|u|^2d\sigma,\qquad
 h={|q_t|^2\over2}-{1\over|q|},
\]
the analytic regular field is
\[
\begin{aligned}
 u_\sigma&=v,\\
 v_\sigma&={h\over2}u+{|u|^2\bar u\over2}G_\epsilon,\\
 h_\sigma&=2\operatorname{Re}(uv\overline{G_\epsilon}),\\
 t_\sigma&=|u|^2.
\end{aligned}
\]
It preserves $2|v|^2-1-h|u|^2=0$ and hence $|v|=1/\sqrt2$ at collision.
The real-analytic chart domain is
$t>0$, $u^2+\epsilon R(t)\ne0$; the latter inequality excludes the other
primary. At collision $t_\sigma=t_{\sigma\sigma}=0$ and
$t_{\sigma\sigma\sigma}=1$, so the radial clock is monotone through the
regularized collision.
Away from collision the physical velocity is
\[
 Z_\theta=\eta\left({2v\over\bar u}+{\epsilon\over2}R_t\right).
\]
Thus increasing regularized time has the opposite physical orientation on
the incoming half.
\end{lemma}

Consequently an isolated collision in the projective family is a zero of the
square map
\[
 (\kappa,\sigma)\longmapsto
 (\operatorname{Re}u,\operatorname{Im}u).
\]
Here the asymptotic normalization of $\kappa$, one LC deck lift, and the
origin and orientation of $\sigma$ are fixed.
Moreover the selected-primary relative angular momentum is exactly
\[
 \ell=q\times q_t=2\det(u,v),\qquad
 \left.\partial_\kappa\ell\right|_{u=0}
 =2\det(\partial_\kappa u,v).
\]
Thus the interval-Newton determinant is precisely half the derivative of a
signed physical impact parameter (with the branch factor $\eta$ when physical
time is restored). With $dt/d\sigma=|u|^2$ fixed it is invariant under a
$\kappa$-dependent LC-time translation and under a deck change; its sign
still depends on the orientation of $\kappa$.
If $D_c=\det(\partial_\kappa u,v)\ne0$ at the zero, the equation
$u\cdot v=0$ defines a unique nearby local closest-passage time. Orthogonal
projection onto $v_c^\perp$ and $|v_c|^2=1/2$ give
\[
 r_{\min,\mathrm{local}}
 =2D_c^2(\kappa-\kappa_c)^2+O(|\kappa-\kappa_c|^3),
 \qquad
 \ell=2D_c(\kappa-\kappa_c)+O(|\kappa-\kappa_c|^2).
\]
This proves local avoidance on both punctured sides of a transverse zero,
without asserting absence of other collisions.
This was the interval-Newton target closed below. The resulting transverse
zero separates nearby continuations locally; identifying full adjacent
collision-free components still requires excluding every remaining collision
zero. Its regularized
continuation is not a physical continuation of a classical finite-mass
collision.

\paragraph{Validated finite stage.}
For $1.26793\le\kappa\le1.26794$, pinned CAPD 6.1.0 native-interval
integration propagates the rigorous stable-tail box from $\zeta=10$ to
$\zeta=0.3$, converts it to the selected-primary LC chart, and propagates the
reversed LC flow for the exact duration $1071249/1000000$. Every accepted LC
step satisfies $t>1$, $R>2$, and distance to the other primary greater than
one. At the final fixed time,
\[
 -0.017807<\operatorname{Re}u<0.016661,
 \qquad
 -0.003953<\operatorname{Im}u<0.004019.
\]
This validates finite propagation and chart safety, but not a collision zero:
the present interval coordinate conversion loses the affine $\kappa$
correlation, so the terminal box is too wide for interval Newton.

\begin{theorem}[Transverse planar collision boundary]
The normalized lower-equilateral planar family with $T=-1$ has exactly one
selected-primary collision zero in
\[
 \begin{aligned}
  1.2679350755&\le\kappa\le1.2679352755,\\
  1.0712484057&\le-\sigma\le1.0712486057.
 \end{aligned}
\]
It lies in
\[
 1.2679351752<\kappa_c<1.2679351759,
 \qquad
 -1.0712485063<\sigma_c<-1.0712485051,
\]
and is transverse, with
\[
 -3.52212<\det(\partial_\kappa u,v)<-3.38401.
\]
\end{theorem}

\begin{proof}
At $\zeta=15$, restriction of the analytic tail enclosure gains the factor
$\lambda^5$, where
\[
 \lambda=\max\{400e^{-15a_T},6500e^{-15a_L}\}<0.04403,
\]
because the state and parameter-tangent corrections all begin in total degree
five. A pinned CAPD $C^1$ doubleton is retained through three fields: reversed
shape flow to $\zeta=0.3$, a unit construction flow writing the exact
nonlinear LC lift, and reversed LC flow. Whole-step enclosures keep both shape
primary distances positive, prove $y>0$ and $u_r,u_i>0$ at the selected lift,
and retain $t>1$, $R>2$, and other-primary distance greater than one.

For $S=-\sigma$, the computed Jacobian columns are
$(\partial_\kappa u,-v)$. Its determinant lies in
\[
 [3.3840110561,3.5221124176].
\]
Rigorous interval Gaussian elimination puts the
interval Newton image strictly inside the displayed parameter-time box,
proving existence and uniqueness there. Restoring increasing $\sigma$ gives
the stated determinant sign. The hexadecimal certificate and pinned replay
are recorded in the companion computer-assisted proof note.
\end{proof}

The adjacent planar component contains a second collision boundary, involving
the opposite primary.

\begin{theorem}[Second planar collision boundary]
The same normalized planar family has exactly one opposite-primary collision
zero in
\[
 \begin{aligned}
 1.2640119181&\le\kappa\le1.2640119321,\\
 1.0275748704&\le S_2\le1.0275749704.
 \end{aligned}
\]
It lies in
\[
 1.2640119191<\kappa_2<1.2640119311,
 \qquad
 1.0275748796<S_2<1.0275749612,
\]
is reached without an earlier collision, and is transverse.  For the fixed
second-chart clock, the interval Jacobian determinant lies in
\[
 98.8377<\det D_{(\kappa,S_2)}F_2<99.2930.
\]
\end{theorem}

\begin{proof}
Propagate the stable-tail enclosure in the positive-primary LC chart for the
exact duration $7/4$.  Whole-step intervals give $t>1$, $R>2$, distance to
the opposite primary greater than one, and selected-primary distance greater
than $10^{-8}$.  Thus this leg is classically collision-free.  At its fixed
endpoint make the exact chart change
\[
 q_-=q+R e_x,
 \qquad
 \dot q_-={2uv\over|u|^2}+{2R\over3t}e_x,
 \qquad u_-^2=q_-.
\]
The switch enclosure proves $\operatorname{Im}q_-<0$, and its image fixes the
exact lift by $\operatorname{Re}u_->1$, $\operatorname{Im}u_-<0$.  In the resulting forced
LC chart the positive primary stays at squared distance greater than $1/100$,
$t>1/2$, and $\operatorname{Re}v_->1/2$ through the candidate collision.
Since $(\operatorname{Re}u_-)_{S}=-\operatorname{Re}v_-<0$ and
$\operatorname{Re}u_->1$ at entry, the terminal zero is the first selected-
pair collision rather than a regularized recollision.  A second $C^1$ doubleton
therefore encloses the square residual
$F_2=(\operatorname{Re}u_-,\operatorname{Im}u_-)$.  Pinned interval Newton
maps the displayed box strictly into
\[
 [1.264011919130,1.264011931053]
 \times[1.027574879669,1.027574961151].
\]
The displayed determinant interval excludes zero, proving existence,
uniqueness in the box, and transversality.  The collision time satisfies
$0.6152<t<0.6153$, so it precedes the prescribed heavy-binary collision.
The hexadecimal certificate is recorded in the second-collision proof note.
\end{proof}

\begin{theorem}[Second-boundary collision-or-escape box]
For every limiting planar parameter
\[
 1.2640099161\le\kappa\le1.2640126461,
\]
the classical branch either collides or, if it remains collision-free,
reaches a strict hyperbolic-escape cone.  The regularized continuation of the
exact collision root reaches that cone.  Every sufficiently small compatible
positive-light-mass member near this box likewise either collides classically
or escapes, and hence has no second brake.
\end{theorem}

\begin{proof}
Continue the negative-primary LC box beyond the preceding root enclosure to a
fixed separated section and set
\[
 q_+=q_--Re_x,\qquad
 \dot q_+={2u_-v_-\over|u_-|^2}-{2R\over3t}e_x.
\]
The interval image proves $\operatorname{Im}q_+<0$.  On that sheet use the
cancellation-free exact lift
\[
 u_{+,i}=-\sqrt{{|q_+|-q_{+,x}\over2}},\qquad
 u_{+,r}={q_{+,y}\over2u_{+,i}},\qquad
 v_+={\overline{u_+}\dot q_+\over2}.
\]
The resulting forced positive-primary LC field has nonsingular forcing
\[
 g_+={e_x\over R^2}-{q_++Re_x\over|q_++Re_x|^3}.
\]
Pinned CAPD carries 195 abutting root-width tiles through this third chart and
then through the signed-$\lambda=\sqrt[3]t$ heavy-binary bridge.  The tile
centers are $1.2640119251+14j\,10^{-9}$, $-143\le j\le51$, and every radius is
$7\times10^{-9}$; their outward-rounded endpoints cover the displayed
interval.  The squared
distances to the unregularized primary in the two post-root LC blocks, and to
either light primary in the bridge, are respectively greater than
\[
 0.1760,\qquad0.1264,\qquad0.004936.
\]
At the terminal section the restricted escape inequality has margin greater
than $2.7591$, while the hierarchical finite-mass inequality has margin
greater than $2.8882$.  Therefore any selected-pair zero is a terminating
classical collision; every member without such a zero agrees with the
regularized flow and escapes.

All preterminal maps form a finite analytic chain of pair-LC charts and a
heavy-pair LC chart, with every nonselected denominator uniformly separated.
Thus the Newtonian-scaled terminal state and inner energy converge for small
positive light mass.  The branch-independent hierarchical-energy lemma
absorbs the full future $O(B)$ tidal work, transferring the strict second
margin.  This proves the positive-mass dichotomy without physically
continuing any collision.  The hexadecimal enclosure and trusted computing
base are recorded in the companion second-boundary escape note.
\end{proof}

\begin{theorem}[Third and fourth planar collision boundaries]
Immediately below the preceding collision-or-escape box, the limiting planar
family has two further classically reached transverse collision roots.  The
positive-primary root satisfies
\[
 \begin{aligned}
  1.2640090993171269&\le\kappa_3\le1.2640090999643190,\\
  0.5884119895288813&\le S_3\le0.5884197454438135,
 \end{aligned}
\]
and
\[
 5224.5883<\det D_{(\kappa,S_3)}F_3<5227.2175.
\]
The subsequent negative-primary root satisfies
\[
 \begin{aligned}
  1.2640090985775962&\le\kappa_4\le1.2640090992806481,\\
  0.3710663877519410&\le S_4\le0.3726139177003617,
 \end{aligned}
\]
and
\[
 417669.25<\det D_{(\kappa,S_4)}F_4<429486.31.
\]
The exact member at the fourth root avoids the preceding positive-primary
collision: outward-rounded interval subtraction gives a separation greater
than $1.2058\times10^{-11}$ between the corresponding root projections.
The ordinal labels refer to the successive encounter charts; uniqueness is
claimed only in the displayed boxes.
\end{theorem}

\begin{proof}
Continue the negative-primary LC chart from the common incoming section and
make the exact positive-primary switch used in the preceding theorem.  In
reversed regularized time, put
\[
 F_3(\kappa,S_3)=(\operatorname{Re}u_+,\operatorname{Im}u_+),
 \qquad \partial_{S_3}u_+=-v_+.
\]
Pinned interval Newton maps the input box
\[
 [1.2640090992,1.2640091000]\times[0.588396,0.588436]
\]
strictly into the first displayed enclosure.  Every earlier selected
distance is positive, the nonselected primary stays separated, and
$\operatorname{Im}v_+<-1/2$ throughout the positive-primary chart.  Hence
$\operatorname{Im}u_+$ increases strictly and the enclosed zero is the first
selected collision.

For the next passage reconstruct
\[
 \lambda=t^{1/3},\quad R=9^{1/3}\lambda^2,\quad
 Z=q_++{R\over2}e_x,\quad V=\dot q_++{R\over3t}e_x,
\]
propagate the signed-$\lambda$ bridge for the exact duration $97/5000$, and
set
\[
 q_-=Z+{R\over2}e_x,
 \qquad \dot q_-=V+{9^{1/3}\over3\lambda}e_x.
\]
On the certified sheet $q_{-,x}>0.05$, the lift
\[
 u_{-,r}=\sqrt{{|q_-|+q_{-,x}\over2}},\qquad
 u_{-,i}={q_{-,y}\over2u_{-,r}},\qquad
 v_-={\overline{u_-}\dot q_-\over2}
\]
is analytic even across $q_{-,y}=0$.  With
$F_4=(\operatorname{Re}u_-,\operatorname{Im}u_-)$, interval Newton maps
\[
 [1.2640090985,1.26400909929]\times[0.37084,0.37284]
\]
strictly into the second displayed enclosure.  The other primary remains at
squared distance greater than $1/10$, and $\operatorname{Re}v_->1/2$ makes
$\operatorname{Re}u_-$ decrease strictly to its first zero.

Finally, a separate interval-Newton replay on
\[
 [1.2640090981,1.2640091001]\times[0.588316,0.588516]
\]
contains every possible intervening positive-primary zero and proves that
its unique root has
\[
 1.2640090992927064\le\kappa_3\le1.2640090999346494.
\]
Subtracting the fourth-root projection with outward rounding gives the
stated positive gap.  The positive chart stays past its zero sheet
thereafter, while all bridge and fourth-chart separations are strict.  Thus
the fourth root is reached classically without the third collision.  The
pinned hexadecimal enclosures and complete chart-domain audit appear in the
companion proof note.
\end{proof}

\begin{theorem}[Outgoing entry after the fourth boundary]
For every
\[
 1.2640090985\le\kappa\le1.26400909929,
\]
the limiting classical branch either collides or reaches a uniformly analytic
fifth positive-primary LC chart.  Along the regularized common continuation,
the intervening negative-chart other-primary squared distance is greater than
$0.333865$, while at the switch
\[
 |u_-|^2>0.213057,\qquad q_{+,x}<-0.577809,
 \qquad |q_+|^2>0.333864.
\]
No terminal escape assertion is made.
\end{theorem}

\begin{proof}
Evaluate the fourth negative-primary chart at the fixed reversed clock
$S_4=0.37284$, which is strictly beyond the preceding root-clock enclosure,
and continue for the exact additional duration $0.42716$.  Pinned whole-step
enclosures give the displayed separation bounds and keep $t>0.05$.  The exact
switch
\[
 q_+=u_-^2-Re_x,\qquad
 \dot q_+={2u_-v_-\over|u_-|^2}-{2R\over3t}e_x
\]
is therefore nonsingular.  Since $q_{+,x}<-0.05$, the sheet
\[
 u_{+,i}=-\sqrt{{|q_+|-q_{+,x}\over2}},qquad
 u_{+,r}={q_{+,y}\over2u_{+,i}}
\]
is uniformly analytic; the interval image has
$u_{+,i}<-0.760137$ and $t>0.240640$.  A selected-pair zero anywhere in the
chain is a terminating classical collision.  Conditional on no such zero,
classical uniqueness identifies the trajectory with the regularized chain
and proves arrival at the fifth chart.  The pinned hexadecimal record appears
in the companion proof note.
\end{proof}

The first root's transversality has an exact positive-mass consequence.  In the full late
Jacobi equations put $A=\sqrt{1-B^2}$, $M=1+A$, and select the same primary by
\[
 q=Z-{A\over M}R.
\]
Direct subtraction gives
\[
 q_{\theta\theta}=-(1+B)\Phi(q)
 +A\{\Phi(R)-\Phi(q+R)\}.
\]
The $R$ equation still contains $B\Phi(q)$, so the common analytic collision
chart uses the colliding-pair center coordinate
\[
 Q=R+{B\over1+B}q.
\]
Then
\[
 Q_{\theta\theta}=-{M+B\over1+B}\left[
 \Phi\left(Q-{B\over1+B}q\right)
 +B\Phi\left(Q+{1\over1+B}q\right)
 \right].
\]
This contains no singular pair force.  After $q=u^2$ and
$dt=|u|^2d\sigma$, the equations for $(Q,Q_t)$ acquire an exterior
$|u|^2$ and are analytic at $u=0$ when $Q\ne0$.
Hence the finite-mass LC constraint is
\[
 2|v|^2-(1+B)-h|u|^2=0,
\]
and $|v|^2=(1+B)/2$ at collision.  At $B=0$, $Q=R$; the validated
inequalities $R>2$ and other-primary distance greater than one therefore
persist on a sufficiently small common chart.

\begin{corollary}[Persistence of the collision boundary]
Fix a regular constrained incoming section before the selected LC block and
extend the limiting coordinate $\kappa$ to a local section coordinate
$\widehat\kappa$.  The certified limiting collision continues uniquely under
all sufficiently small positive-mass and compatible section-state
perturbations $\delta$.  The continued root
$(\widehat\kappa_c(B,\delta),\sigma_c(B,\delta))$ is transverse.  At fixed
$(B,\delta)$ its local closest-passage normal form is
\[
 r_{\min,\mathrm{local}}
 ={2D_{B,\delta}^2\over1+B}
  (\widehat\kappa-\widehat\kappa_c(B,\delta))^2
  +O(|\widehat\kappa-\widehat\kappa_c(B,\delta)|^3),
\]
where $D_{B,\delta}=\det(\partial_{\widehat\kappa}u,v)$ remains negative.
\end{corollary}

\begin{proof}
The constraint, gauge, and section functions have constant full rank at the
certified collision-free base state, so the fixed constrained section is
smooth also at $B=0$; equivalently use the analytic signed-$B$ extension and
then restrict to $B\ge0$.  On this section choose a $C^1$ chart
$X_{\rm in}(B,\widehat\kappa,\delta)$ which restricts at $(B,\delta)=0$ to
the validated limiting curve.  Ordinary pre-LC flow, the strict square-root
lift, and the collision-adapted $(u,Q)$ flow make the square shooting map
$C^1$ in $(B,\widehat\kappa,\delta,\sigma)$.  The parameter-dependent
implicit-function theorem and the strict determinant bound give existence,
local uniqueness, and transversality.  Orthogonal projection onto $v^\perp$,
followed by $q=u^2$ and $|v|^2=(1+B)/2$, gives the displayed coefficient.
\end{proof}

\begin{theorem}[Differentiated joint inclination]
Let $K$ be a compact projective-amplitude interval inside the validated
pre-collision chart and set $p=\mu/\tau$.  In the normalized family
$H=\kappa|T|^p$, with stable entrance coordinate $S$ fixed, translate the
logarithmic saddle dwell by $L_T=-\tau^{-1}\log|T|$ and scale size by
$\rho_T=|T|^{1/\tau}$.  If
\[
 B|\log|T||+B+|T|^{2-p}+e^{-cL_T}
 +\|\widehat\Xi\|\longrightarrow0,
\]
where $\widehat\Xi$ contains the Newtonian-scaled energy, clock, and remaining
triangular deviations, then the state and its $\kappa$-tangent converge on
every common collision-free compact interval.  Through the selected limiting
collision the same assertion holds in the scaled $(u,v,Q,P,h)$
Levi--Civita chart.

Consequently the certified collision root extends uniquely as a $C^1$ graph
\[
 \kappa=\kappa_c(B,T,S,\Xi),
 \qquad \kappa_c(B,T,S,\Xi)\longrightarrow\kappa_c.
\]
Its determinant retains the certified negative sign.  At fixed parameters,
\[
 r_{\min,\mathrm{local}}
 =\rho_T{2\widehat D^2\over1+B}(\kappa-\kappa_c)^2
  +o\!\left(\rho_T|\kappa-\kappa_c|^2\right).
\]
\end{theorem}

\begin{proof}
Choose a fixed weight $\mu/2<\omega<\tau$.  For small $B$ it also satisfies
$\mu_B/2<\omega<\tau_B$, and the quadratic Lyapunov--Perron Green denominator
is bounded below by $2\omega-\mu_B>0$.  Construct only the nonlinear
unstable-manifold shadow on the infinite weighted tail.  Translation changes
its two homogeneous coefficients by
$e^{(\mu_B-\mu)L_T}$ and $e^{(\tau_B-\tau)L_T}$; these tend to one because
$B L_T\to0$.  Differentiating the uniformly contracting fixed-point equation
gives convergence of the $\kappa$-tangent.

A bounded finite-entrance stable datum does not belong to that half-line
weighted ball.  Instead, $C^1$ stable-foliation holonomy compares the actual
orbit and tangent with the shadow at one fixed incoming-tail section, with
error $O(e^{-c(L_T-A)}+\rho_T|E|)+o(1)$.  Compact variational flow and the
zero-angular-momentum fixed-axis reconstruction propagate this estimate.
The scaled collision-adapted $Q$--LC vector field is analytic at $u=0$ and
propagates the variational equation through the regularized zero.  The
parameter-dependent implicit-function theorem and the certified determinant
give the graph.  The $C^1$ expansion of $u$ transverse to the root, together
with $q=u^2$ and $|v|^2=(1+B)/2$, gives the last formula.
\end{proof}

\begin{theorem}[Real collision sampling]
There are infinitely many distinct real parameters on the tied normalized
Pythagorean curve, accumulating at the skinny endpoint, whose classical
solution has a finite-time binary or triple collision.
\end{theorem}

\begin{proof}
Let $\chi_*$ be the asymptotic intercept of the certified transverse
intersection between the restricted incoming parabolic curve and the
collision-stable curve.  In the signed longitudinal amplitude $H_0$, choose
two compact intercept arcs on opposite sides of $\chi_*$ with fixed strict
opposite signs.  Since
\[
 \Phi_{\rm ref}'(B)=-{3\pi\over2}B^{-5/2}(1+o(1)),
\]
every sufficiently late phase winding supplies a disjoint real $B$-bracket
whose reference intercept crosses these arcs.  If any member collides before
the selected late chart, that bracket already supplies the theorem.  If not,
uniform incoming-tail convergence and compact regularized flow transfer the
two $H$-signs to the positive-mass saddle entrance.

Put $p=\mu/\tau$.  Continuity gives, inside the bracket, a last zero of $H$
followed by a first point at which $H=B^{p/2}$.  On the intervening segment
$0\le H\le B^{p/2}\to0$.  Local uniqueness of the transverse restricted
intersection therefore forces every sequence in this segment to approach the
certified endpoint.  Uniformly there,
\[
 {T_B\over B}\longrightarrow\Theta_*\ne0,\qquad
 \|\widehat\Xi_B\|\longrightarrow0,
\]
so the normalized collision graph is defined.  The continuous function
\[
 G(B)=H_B-|T_B|^p\kappa_c(B,T_B,S_B,\Xi_B)
\]
is negative at $H=0$ because $\kappa_c>0$, and positive at
$H=B^{p/2}$ because $|T_B|^p=O(B^p)=o(B^{p/2})$.  Its intermediate-value
zero is an exact selected light--heavy collision of the tied positive-mass
trajectory.  The phase brackets are pairwise disjoint and tend to zero.
\end{proof}

\begin{theorem}[Explicit local collision-or-escape cusp]
Every regularized limiting member in
\[
 1.2679251755\le\kappa\le1.2679451755
\]
reaches the strict terminal escape cone described below.  There is a
neighborhood of this closed interval such that every sufficiently small
compatible positive-light-mass member either has a finite classical collision
before the terminal section or its light body escapes to infinity.  Hence
every collision-free member in this local cusp has no second brake.
\end{theorem}

\begin{proof}
Ten adjacent pinned interval-boxed propagations cover the full displayed
\(\kappa\)-interval, cross any selected collision in its Levi--Civita chart,
and reconstruct the restricted outer state at a fixed ejection section.  With
\(\lambda=\sqrt[3]t\), a second analytic LC bridge crosses the prescribed
heavy-binary collision.  Whole-step enclosures keep both light--primary
squared distances above \(0.89\).  On the outgoing section they prove the
strict restricted escape inequality
\[
 \dot\rho_0-{2\over 2w_0}-2-a'(s_0)>0.54.
\]

For the positive-mass transfer, let \(x\) be the heavy-pair Jacobi vector,
\(y\) the light position relative to its center of mass, and put
\[
 r=|x|,\quad \rho=|y|,\quad w=\rho-r,\quad
 e={1\over2}|\dot x|^2-{M\over r},\quad
 M=m_a+m_b,\quad\mathcal M=M+m_c.
\]
The exact Jacobi equations give
\[
 |\ddot y|\le{\mathcal M\over w^2},\qquad
 |\dot e|\le{2m_c r|\dot x|\over w^3}.
\]
Fix \(\epsilon,c>0\), set
\[
 v_b=\sqrt{{2M\over r_0}+2\epsilon},
\quad
 K=\int_0^\infty
 {\sqrt{2M(r_0+v_bt)+2\epsilon(r_0+v_bt)^2}
  \over(w_0+ct)^3}\,dt.
\]
If
\[
 \dot\rho_0-{\mathcal M\over cw_0}>v_b+c,\qquad
 e_0+2m_cK<\epsilon,
\]
then a simultaneous bootstrap gives
\[
 r(t)\le r_0+v_bt,\qquad
 \dot\rho(t)>v_b+c,\qquad w(t)\ge w_0+ct,\qquad e(t)<\epsilon.
\]
Thus only an inner collision can interrupt outer escape.  In the certified
massless terminal state \(M=\mathcal M=2\), \(m_c=0\), and \(e_0=0\).
For \(\epsilon=1/100\), \(c=3/2\), interval propagation proves the stronger
full-pair margin
\[
 \dot\rho_0-{2\over(3/2)(\rho_0-r_0)}
 -\sqrt{{4\over r_0}+{2\over100}}-{3\over2}>0.65.
\]
Apply the comparison in the Newtonian-scaled finite-mass terminal
coordinates, where \(\widehat e=\rho_Te\).  Scaled LC convergence gives
\(\widehat e_0\to0\), while the correspondingly scaled \(K\) is uniformly
bounded and \(m_cK=O(B)\).  The strict inequalities persist.
An exact positive-mass collision terminates the classical branch; otherwise
the member reaches the terminal section and escapes.
\end{proof}

\begin{corollary}[Open-window refinement]
Every sufficiently late skinny phase-cycle bracket either contains a
classical collision before the common selected chart, or contains a nonempty
open tied parameter interval every member of which collides or escapes.
\end{corollary}

\begin{proof}
On a bracket without an earlier collision, use the segment in the real
collision-sampling proof from \(H=0\) to \(H=B^{p/2}\).  The flow makes
\(H,T\) continuous, while the uniform estimate
\(T/B\to\Theta_*\ne0\) keeps the denominator nonzero.  Thus the continuous
normalized coordinate satisfies
\[
 {H\over|T|^p}=0
 \quad\hbox{at the first endpoint},\qquad
 {H\over|T|^p}\asymp B^{-p/2}\longrightarrow+\infty
 \quad\hbox{at the second}.
\]
It therefore traverses the interior of
\([1.2679251755,1.2679451755]\).  The preimage of that interior is a nonempty
open set and hence contains an open interval.  Restricted to this preimage,
compactness of the projective-amplitude interval, uniform endpoint
localization, and differentiated regularized convergence supply the remaining
finite-mass cusp hypotheses.  The preceding theorem classifies every member
of this open set as collision or escape.
\end{proof}

This boundary emanates from the massless planar joint-limit collision and
persists in the surrounding finite-mass section family. It is not a tied
finite-mass Pythagorean orbit or a brake, and no positive-mass classical
collision is continued physically.  The preceding theorem classifies a
neighborhood of this first root. What remains is exclusion or certification
of the remaining collision zeros and outgoing control on the compact
components.  In
particular, the exact tied trajectory selects only one point
$(\widehat\kappa_B,\delta_B)$ on the fixed section; the collision-boundary
persistence corollary does not prove that it solves
$\widehat\kappa_B=\widehat\kappa_c(B,\delta_B)$.  The theorem replaces this
section-dependent condition by the intrinsic scalar gap
\[
 \Delta_{\rm intr}(B)=
 {H_B\over|T_B|^{\mu/\tau}}
 -\kappa_c(B,T_B,S_B,\Xi_B).
\]
No componentwise sign classification of this gap is proved here.  The real
collision-sampling theorem gives only a cyclewise dichotomy between an earlier
collision and a zero of this selected graph.  Its isolated roots are not
proved rational.

Parameterize the restricted incoming parabolic curve by its asymptotic phase
intercept $\chi$, normalized by
$\chi=\lim_{Y\to\infty}(\phi_Y+(4/3)Y^{3/2})$ modulo $2\pi$. This coordinate
includes the finite restricted quadrupole correction between infinity and a
fixed section; a raw monopole section phase does not. The incoming-tail
theorem inserts the true full regularized state, producing infinitely many
open terminal nonperiodicity intervals accumulating at
$B=0$ result. Strict restricted escape on a compact intercept arc
transfers to a finite outgoing scaled section; there
$e=-C/B+o(B^{-1})$ and $\dot\rho=sB^{-1/2}+o(B^{-1/2})$, so the terminal
escape lemma applies uniformly, yielding escape or an earlier classical inner
collision and excluding a later brake in either case.

\begin{theorem}[Infinite subfamily]
There are infinitely many open real Euclid-parameter intervals accumulating
at $u=0$ on which the Pythagorean--Burrau solution is nonperiodic. In
particular, infinitely many distinct primitive Pythagorean triples satisfy the
nonperiodicity conjecture.
\end{theorem}
\begin{proof}
Shrink a restricted escape-intercept arc by a fixed positive margin. The
incoming-tail theorem and robust terminal transfer cover every sufficiently
small preimage interval. A trajectory that collides before section reach is
already nonperiodic; every other trajectory satisfies the terminal
escape-or-collision certificate. The exact derivative
$\Phi_{\rm ref}'\sim-(3\pi/2)B^{-5/2}$ gives infinitely many disjoint
preimage intervals. Since $B(u)=2u/(1+u^2)$ is a homeomorphism near zero and
rational $u$ is dense, each contains a rational Euclid parameter. Reduction
by the simultaneous mass--length scaling gives distinct primitive triples.
\end{proof}

The existential rational consequence can be sharpened to one explicit
primitive family.

\begin{theorem}[Positive density in an explicit family]
Let
\[
(a_n,b_n,c_n)=(4n^2-1,4n,4n^2+1),\qquad n\ge1.
\]
There is a positive-lower-natural-density set of indices $n$ for which the
corresponding Pythagorean--Burrau solution is nonperiodic.
\end{theorem}
\begin{proof}
Here $u_n=1/(2n)$ and $B_n=4n/(4n^2+1)$. Direct substitution in the exact
reference clock gives
\[
F(x):={\Phi_{\rm ref}(B_x)\over2\pi}
={ (2x-1)^{3/2}(2x+1)(4x^2+1)^{3/4}\over32x^{5/2}},
\]
and therefore
\[
F''(x)={3\over8}x^{-1/2}+{1\over32}x^{-3/2}+O(x^{-5/2}).
\]
On each dyadic block, van der Corput's second-derivative estimate makes every
nontrivial Weyl sum $O_h(N^{3/4})$. Weyl's criterion proves that $F(n)$ is
equidistributed modulo one. Consequently every fixed compact escape-intercept
arc $J$ selects a set of indices of density $|J|/(2\pi)$. The incoming-tail
and terminal-transfer theorems prove every sufficiently large selected member
nonperiodic. Primitivity follows from
$\gcd(4n,4n^2-1)=1$.
\end{proof}

\section{Cardinality and the remaining obstruction}

The rational parameters are countable and dense.  Neither countability,
measure zero, analyticity, nor the nominal three-equations/two-unknowns count
excludes rational zeros.  A proof still requires either no real zero, an exact
arithmetic characterization of all real zeros, or a finite exact reduction.

The active route factors a possible brake, away from Jacobi degeneracies, into
two radial-rate equations followed by an angular-slip equation, and seeks to
classify the isolated double-radial events along the first-syzygy continuation.
Ordinary scans show both signs of angular slip on different double-radial
events, ruling out a universal fixed-sign shortcut. The skinny route now
controls the full incoming plunge and proves open escape windows. On the
finite-turn side, fixed-resonance first-order matching and a uniform outgoing
angular-momentum theorem exclude a brake at the first outer turn throughout
the infinite accumulating resonance layer. The returning-tail estimate
propagates the sign back to a fixed large section; the next problem is to
classify the resulting compact collision-regularized central-encounter map.

\section{Necessary integral identities}

For a hypothetical second brake at $\tau$, the Lagrange--Jacobi identity is
\[
\ddot I=4K-2U=2U-4U_0.
\]
Both endpoint dilational momenta vanish, so
\[
\int_0^\tau Udt=2U_0\tau,\qquad
\int_0^\tau Kdt=U_0\tau.
\]
Integrating each labelled acceleration also gives zero.  If
$d_{ij}=q_j-q_i$, multiplication by the fixed initial positions yields the
additional scalar condition
\[
\int_0^\tau\sum_{i<j}m_im_j
{d_{ij}(0)\cdot d_{ij}(t)\over r_{ij}(t)^3}dt=0.
\]
Its integrand is initially positive, but no global projection inequality has
been proved through close encounters.

At energy $-U_0$, the Jacobi--Maupertuis metric is
$2(U-U_0)ds_m^2$.  A second-brake segment is a degenerate-boundary geodesic
chord.  A Morse-index or convexity obstruction remains open.

\section{Catalog comparison}

Li and Liao list a nearby periodic brake orbit on the mass slice
$(0.6,0.8,1)$.  The exact tied point is $(x,y)=(0.14,0.48)$, while their
closest listed entry is $(0.1446319096,0.4773197126)$.  Its side lengths and
right-triangle defect do not satisfy the exact equations.  Equal-mass modern
catalogs are also disjoint from the tied right-triangle curve.  No inspected
catalog supplies a validated exact counterexample.  Local shooting continuation
of the $F_{30}$ family varies its masses and reduces the tied-shape gap to about
$0.00369$, but the two shape-matching coordinates cross zero at distinct mass
parameters.  This is ordinary numerical evidence, not validated
nonintersection.

\section{Open proof obligations}

The rational and strong real conjectures remain unresolved.  The missing
theorem must control arbitrarily close collision-free encounters, classify or
exclude every double-radial event including Jacobi degeneracies, and be uniform
at parameter endpoints.  If real intersections remain, an exact arithmetic
bridge is still required. In the planar joint-limit family, two transverse
collision boundaries are now certified, one for each primary, while the first
has a classified local finite-mass collision-or-escape cusp.  Global use
requires exclusion or certification of the remaining collision zeros and
outgoing control on every remaining collision-free component.  Differentiated
transport through the finite-mass saddle dwell is complete; on the exact tied curve the remaining
local condition is the scalar
sign/zero problem for $\Delta_{\rm intr}(B)$.  Phase sampling forces infinitely
many real classical collisions but does not classify each collision-free
component or yield a rational root.

\begin{thebibliography}{9}
\bibitem{Burrau1913} C. Burrau, Astronomische Nachrichten 195 (1913), 113.
\bibitem{Szebehely1967} V. Szebehely, Burrau's problem of three bodies,
Proc. Natl. Acad. Sci. USA 58 (1967), 60--65.
\bibitem{SP1967} V. Szebehely and C. F. Peters, Complete solution of a general
problem of three bodies, Astronomical Journal 72 (1967), 876.
\bibitem{MMV} R. Moeckel, R. Montgomery, and A. Venturelli, From brake to
syzygy, Arch. Ration. Mech. Anal. 204 (2012), 1009--1060.
\bibitem{LiLiao} X. Li and S. Liao, Collisionless periodic orbits in the
free-fall three-body problem, New Astronomy 70 (2019), 22--26.
\bibitem{Standish} E. M. Standish, Sufficient conditions for escape in the
three-body problem, Celestial Mechanics 4 (1971), 44--48.
\bibitem{Yoshida} J. Yoshida, Improved criteria for hyperbolic--elliptic
motion in the general three-body problem, PASJ 24 (1972), 391--408.
\bibitem{BakkerSimmons} L. F. Bakker and S. C. Simmons, A separating surface
for Sitnikov-like $n+1$-body problems, J. Differential Equations 258 (2015),
3063--3087.
\bibitem{McGehee1973} R. McGehee, A stable manifold theorem for degenerate
fixed points with applications to celestial mechanics, J. Differential
Equations 14 (1973), 70--88.
\end{thebibliography}

\end{document}
