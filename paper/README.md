# Working paper

`main.tex` is intentionally titled as an exact-reductions draft until the
global nonperiodicity theorem is earned.  Compile with:

```bash
cd paper
pdflatex -interaction=nonstopmode main.tex
pdflatex -interaction=nonstopmode main.tex
```

