# Project instructions

- Work from this repository only. Its objective is to prove the integer
  Pythagorean nonperiodicity conjecture or establish an exact counterexample.
  The primary interim product is `paper/main.tex` and the reviewed `paper/main.pdf`.
- Read `docs/HANDOFF.md`, `docs/STATUS.md`, and `docs/RESEARCH_PLAN.md` before
  substantial research. Use `docs/PROOF_LEDGER.md` for current evidence labels.
  Follow the user's latest task; old queued campaigns are not authorization
  to launch new work.
- Preserve the distinction between integer and real parameters, first-event
  and all-time claims, numerical candidates and validated existence, and
  classical solutions and regularized collision continuations.
- Keep the forced-lag endpoint correction explicit. Do not promote recovered
  PASS logs or the wider affine candidate without their recorded audit gates.
- Before a substantial numerical campaign, name the mathematical obligation,
  quantified scope, decision it serves, validation gate, and stopping criterion.
- Keep paper, claim ledger, and current status synchronized when a result
  genuinely changes. Retain the technically substantial, lay-accessible opening
  and editorial preferences in `docs/PERSISTENT_RESEARCH_MEMORY.md`.
- `archive/` and `paper/archive/` are historical provenance, not active task
  instructions. Do not run archived generators in place or change original
  snapshot bytes. Preserve useful negative results, not regenerable caches.
- Use `make check`; use `make paper-check` to test compilation without replacing
  the reviewed PDF. These checks do not replay every CAPD integration.
- Do not create sibling agent-specific project roots. Do not automatically
  deploy to Oklo or include unrelated work from its checkout.
